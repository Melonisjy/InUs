/**
 * 루트 페이지
 * 
 * 앱 진입점. 온보딩으로 리다이렉트하거나
 * 첫 경험을 제공하는 페이지.
 */

import { Screen } from '@/shared/ui/layouts'

export default function HomePage() {
  return (
    <Screen>
      home
    </Screen>
  )
}
